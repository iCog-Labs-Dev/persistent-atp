# Local runtime dependencies for pip/uv compatibility
# This file mirrors the primary project dependencies for simple installs.
python-dotenv>=1.0
neo4j>=5.14

# Dev dependencies
pytest>=8.0
jsonschema>=4.0
